// 1. Erstellen Sie ein Set namens farbenSet
let farbenSet = new Set(['Gelb', 'Grün', 'Blau']);

// 2. Iterieren Sie mit einer for...of -Schleife
for (let farbe of farbenSet) {
    console.log(`Farbe: ${farbe}`);
}